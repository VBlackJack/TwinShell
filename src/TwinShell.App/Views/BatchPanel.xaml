<UserControl x:Class="TwinShell.App.Views.BatchPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Batch Execution" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="Execute multiple commands sequentially" FontSize="12" Opacity="0.7" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Batch List -->
        <GroupBox Grid.Row="1" Header="Available Batches" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListBox Grid.Row="0" ItemsSource="{Binding Batches}" SelectedItem="{Binding SelectedBatch}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Description}" Opacity="0.7" FontSize="11" Margin="0,2,0,0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                    <TextBlock Text="{Binding CommandCount, StringFormat='Commands: {0}'}" FontSize="10" Opacity="0.6"/>
                                    <TextBlock Text=" | " FontSize="10" Opacity="0.6" Margin="5,0"/>
                                    <TextBlock Text="{Binding ExecutionMode}" FontSize="10" Opacity="0.6"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Buttons -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,0">
                    <Button Content="▶ Execute" Command="{Binding ExecuteBatchCommand}" MinWidth="100" Margin="0,0,5,0"/>
                    <Button Content="Import" Command="{Binding ImportBatchCommand}" MinWidth="80" Margin="0,0,5,0"/>
                    <Button Content="Export" Command="{Binding ExportBatchCommand}" MinWidth="80" Margin="0,0,5,0"/>
                    <Button Content="Delete" Command="{Binding DeleteBatchCommand}" MinWidth="80"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Progress -->
        <GroupBox Grid.Row="2" Header="Progress" Margin="0,0,0,10" Visibility="{Binding IsExecuting, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="{Binding ProgressMessage}" Margin="0,0,0,5"/>
                <ProgressBar Value="{Binding ProgressPercentage}" Height="20" Minimum="0" Maximum="100"/>
            </StackPanel>
        </GroupBox>

        <!-- Output -->
        <GroupBox Grid.Row="3" Header="Output">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#1E1E1E">
                <ItemsControl ItemsSource="{Binding OutputLines}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Text}"
                                     Foreground="{Binding IsError, Converter={StaticResource ErrorToColorConverter}}"
                                     FontFamily="Consolas"
                                     FontSize="11"
                                     Margin="5,1"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>
